# ==============================================================================
# Codemagic CI/CD for Sunfire Manga Reader — v3.0.0-beta
# Supports: Android (Universal & Split APKs) & iOS (Unsigned Sideload IPA)
# ==============================================================================

workflows:

  # ────────────────────────────────────────────────────────────────────────────
  # WORKFLOW 1: ANDROID RELEASE (Linux x2 — Fast & Lightweight)
  # ────────────────────────────────────────────────────────────────────────────
  android-release:
    name: Android Release (Universal & Split APKs) - v3.0.0-beta
    max_build_duration: 60
    instance_type: linux_x2
    environment:
      flutter: stable
      java: 17
      vars:
        LANG: en_US.UTF-8
        LC_ALL: en_US.UTF-8
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/.gradle/caches
        - $HOME/.gradle/wrapper
    scripts:
      - name: Set up Android SDK local.properties
        script: |
          echo "flutter.sdk=$FLUTTER_ROOT" > "$CM_BUILD_DIR/android/local.properties"
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build Android Universal APK
        script: |
          set -e
          VERSION=$(grep '^version:' "$CM_BUILD_DIR/pubspec.yaml" | sed -e 's/version: //' -e 's/["'\'']//g' -e 's/+.*//' -e 's/ //g')
          flutter build apk --release --android-skip-build-dependency-validation
          cd build/app/outputs/flutter-apk
          if [ -f app-release.apk ]; then
            cp app-release.apk Sunfire-universal.apk
            cp app-release.apk "Sunfire-v${VERSION}-universal.apk"
          fi
      - name: Build Android Split-per-ABI APKs
        script: |
          set -e
          VERSION=$(grep '^version:' "$CM_BUILD_DIR/pubspec.yaml" | sed -e 's/version: //' -e 's/["'\'']//g' -e 's/+.*//' -e 's/ //g')
          flutter build apk --release --split-per-abi --android-skip-build-dependency-validation
          cd build/app/outputs/flutter-apk
          [ -f app-arm64-v8a-release.apk ] && cp app-arm64-v8a-release.apk Sunfire-arm64-v8a.apk && cp app-arm64-v8a-release.apk "Sunfire-v${VERSION}-arm64-v8a.apk"
          [ -f app-armeabi-v7a-release.apk ] && cp app-armeabi-v7a-release.apk Sunfire-armeabi-v7a.apk && cp app-armeabi-v7a-release.apk "Sunfire-v${VERSION}-armeabi-v7a.apk"
          [ -f app-x86_64-release.apk ] && cp app-x86_64-release.apk Sunfire-x86_64.apk && cp app-x86_64-release.apk "Sunfire-v${VERSION}-x86_64.apk"
          ls -lh Sunfire*.apk || ls -lh *.apk
    artifacts:
      - build/app/outputs/flutter-apk/Sunfire*.apk
      - build/app/outputs/flutter-apk/*.apk

  # ────────────────────────────────────────────────────────────────────────────
  # WORKFLOW 2: iOS RELEASE (Mac Mini M2 — AltStore / TrollStore / Sideloadly)
  # ────────────────────────────────────────────────────────────────────────────
  ios-release:
    name: iOS IPA (Unsigned Sideload) - v3.0.0-beta
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        LANG: en_US.UTF-8
        LC_ALL: en_US.UTF-8
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/Library/Caches/CocoaPods
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build iOS xcarchive (No code signing required)
        script: |
          set -e
          flutter build ipa --release --no-codesign
      - name: Package Unsigned Sunfire.ipa
        script: |
          set -e
          VERSION=$(grep '^version:' "$CM_BUILD_DIR/pubspec.yaml" | sed -e 's/version: //' -e 's/["'\'']//g' -e 's/+.*//' -e 's/ //g')
          mkdir -p build/ios/ipa
          shopt -s nullglob
          ipas=(build/ios/ipa/*.ipa)
          if [ ${#ipas[@]} -gt 0 ]; then
            echo "Existing IPA found from flutter build: ${ipas[0]}"
            mv "${ipas[0]}" build/ios/ipa/Sunfire.ipa
          else
            echo "Packaging Sunfire.ipa directly from xcarchive payload..."
            APP_DIR=$(find build/ios/archive -type d -name "*.app" | head -n 1)
            if [ -n "$APP_DIR" ]; then
              echo "Found app payload at: $APP_DIR"
              rm -rf /tmp/sunfire-ipa && mkdir -p /tmp/sunfire-ipa/Payload
              cp -R "$APP_DIR" /tmp/sunfire-ipa/Payload/
              (cd /tmp/sunfire-ipa && zip -qr "$CM_BUILD_DIR/build/ios/ipa/Sunfire.ipa" Payload)
              echo "✅ Successfully created Sunfire.ipa"
            else
              echo "❌ Error: Could not find .app bundle in build/ios/archive!" >&2
              exit 1
            fi
          fi
          cp build/ios/ipa/Sunfire.ipa "build/ios/ipa/Sunfire-v${VERSION}.ipa"
          ls -lh build/ios/ipa/Sunfire*.ipa
    artifacts:
      - build/ios/ipa/Sunfire*.ipa
      - build/ios/archive/*.xcarchive

  # ────────────────────────────────────────────────────────────────────────────
  # WORKFLOW 3: ALL PLATFORMS (Unified CI — Triggered on Tags and Main)
  # ────────────────────────────────────────────────────────────────────────────
  all-release:
    name: Multi-Platform Release (APK & IPA) - v3.0.0-beta
    max_build_duration: 90
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      java: 17
      cocoapods: default
      vars:
        LANG: en_US.UTF-8
        LC_ALL: en_US.UTF-8
    triggering:
      events:
        - tag
        - push
      branch_patterns:
        - pattern: 'main'
          include: true
          source: true
      tag_patterns:
        - pattern: 'v*'
          include: true
    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/.gradle/caches
        - $HOME/.gradle/wrapper
        - $HOME/Library/Caches/CocoaPods
    scripts:
      - name: Set up Android SDK local.properties
        script: |
          echo "flutter.sdk=$FLUTTER_ROOT" > "$CM_BUILD_DIR/android/local.properties"
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build Android Universal & Split APKs
        script: |
          set -e
          VERSION=$(grep '^version:' "$CM_BUILD_DIR/pubspec.yaml" | sed -e 's/version: //' -e 's/["'\'']//g' -e 's/+.*//' -e 's/ //g')
          flutter build apk --release --android-skip-build-dependency-validation
          cd build/app/outputs/flutter-apk
          if [ -f app-release.apk ]; then
            cp app-release.apk Sunfire-universal.apk
            cp app-release.apk "Sunfire-v${VERSION}-universal.apk"
          fi
          cd "$CM_BUILD_DIR"
          flutter build apk --release --split-per-abi --android-skip-build-dependency-validation
          cd build/app/outputs/flutter-apk
          [ -f app-arm64-v8a-release.apk ] && cp app-arm64-v8a-release.apk Sunfire-arm64-v8a.apk && cp app-arm64-v8a-release.apk "Sunfire-v${VERSION}-arm64-v8a.apk"
          [ -f app-armeabi-v7a-release.apk ] && cp app-armeabi-v7a-release.apk Sunfire-armeabi-v7a.apk && cp app-armeabi-v7a-release.apk "Sunfire-v${VERSION}-armeabi-v7a.apk"
          [ -f app-x86_64-release.apk ] && cp app-x86_64-release.apk Sunfire-x86_64.apk && cp app-x86_64-release.apk "Sunfire-v${VERSION}-x86_64.apk"
          ls -lh Sunfire*.apk || ls -lh *.apk
          cd "$CM_BUILD_DIR"
      - name: Build iOS unsigned xcarchive & IPA
        script: |
          set -e
          VERSION=$(grep '^version:' "$CM_BUILD_DIR/pubspec.yaml" | sed -e 's/version: //' -e 's/["'\'']//g' -e 's/+.*//' -e 's/ //g')
          flutter build ipa --release --no-codesign
          mkdir -p build/ios/ipa
          shopt -s nullglob
          ipas=(build/ios/ipa/*.ipa)
          if [ ${#ipas[@]} -gt 0 ]; then
            mv "${ipas[0]}" build/ios/ipa/Sunfire.ipa
          else
            APP_DIR=$(find build/ios/archive -type d -name "*.app" | head -n 1)
            if [ -n "$APP_DIR" ]; then
              rm -rf /tmp/sunfire-ipa && mkdir -p /tmp/sunfire-ipa/Payload
              cp -R "$APP_DIR" /tmp/sunfire-ipa/Payload/
              (cd /tmp/sunfire-ipa && zip -qr "$CM_BUILD_DIR/build/ios/ipa/Sunfire.ipa" Payload)
            fi
          fi
          cp build/ios/ipa/Sunfire.ipa "build/ios/ipa/Sunfire-v${VERSION}.ipa"
          ls -lh build/ios/ipa/Sunfire*.ipa
    artifacts:
      - build/app/outputs/flutter-apk/Sunfire*.apk
      - build/app/outputs/flutter-apk/*.apk
      - build/ios/ipa/Sunfire*.ipa
      - build/ios/archive/*.xcarchive
