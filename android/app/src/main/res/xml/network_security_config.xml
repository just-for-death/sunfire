<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- FOSS: no cleartext by default. Do not set usesCleartextTraffic on
         <application> (scanners treat that as a dangerous manifest bit). -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Loopback / emulator host only. Android NSC cannot express RFC1918
         CIDR; use HTTPS or a reverse proxy for LAN Suwayomi on private IPs. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
